# Core ML stack — Colab T4 friendly (May 2026; Colab ships torch 2.10 + CUDA 12.8).
# Do NOT pin torch here. The pre-installed Colab torch + torchvision + torchaudio
# trio is matched to the runtime CUDA; downgrading torch breaks torchvision ops.
transformers>=4.50.0,<5.0.0
datasets>=2.20.0,<3.0  # MLSUM still uses a loader script; pinned <3.0 until HF migrates it
accelerate>=1.1.0
peft>=0.13.0
sentencepiece>=0.2.0
protobuf>=4.25.5

# Teacher LLM clients
openai>=1.55.0
anthropic>=0.40.0

# Evaluation
rouge-score>=0.1.2
bert-score>=0.3.13
sacrebleu>=2.4.0

# Utilities
pyyaml>=6.0
tqdm>=4.67.0
python-dotenv>=1.0.1
tenacity>=9.0.0
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0
scikit-learn>=1.5.0

# Demo
gradio>=5.0.0
